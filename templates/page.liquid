<div class="page-content">
  <div class="container">
    <h1>{{ page.title }}</h1>
    <div class="page-body">{{ page.content }}</div>
  </div>
</div>
<style>
.page-content { padding: 100px 0 60px; min-height: 60vh; }
.page-content h1 { font-size: 2rem; font-weight: 800; color: #1B2A4A; margin-bottom: 32px; }
.page-body { max-width: 800px; line-height: 1.8; color: #444; }
.page-body h2, .page-body h3 { color: #1B2A4A; margin: 24px 0 12px; }
.page-body ul, .page-body ol { margin: 12px 0 12px 24px; }
.page-body li { margin-bottom: 6px; }
.page-body a { color: #FF6B35; }
.page-body hr { border: none; border-top: 1px solid #dee2e6; margin: 24px 0; }
</style>
